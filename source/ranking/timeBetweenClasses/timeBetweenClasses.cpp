#include "timeBetweenClasses.hpp"


int timeBetweenClasses(schedule * s)
{
    int gapCount = 0;
    s->sortSchedule();
    for(int i = 0; i < s->getCourseCount() -1; i++){
        for(int j = 0; j < s->getCourseCount(); j++)
        {
            
        }
    }
}