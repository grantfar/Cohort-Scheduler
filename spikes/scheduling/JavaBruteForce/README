This is the Java implementation of the Cohort scheduling algorithm.
In order to run it, you mus download it, and import as a project to Eclipse or a similar IDE. 
Currently, the only output is to the console window in the IDE.